<div class="pull left">
  <div class="sidebar">
    <% if logged_in? %>
    <ul>
    <% @chatrooms.each do |room| %>
      <li><%= link_to room.name.upcase, chatroom_path(id: room.id)%></li>
    <% end %>

  </ul>
  <% end %>
  </div>
</div>
<% if logged_in? %>
<h1>Please select a room to view</h1>
<% else %>

<div class="center jumbotron">
    <h2>Welcome to SLAcKk</h2>

    <%= link_to "Sign up now!", sign_up_path, class: "btn btn-lg btn-primary" %>
  </div>
<% end %>
